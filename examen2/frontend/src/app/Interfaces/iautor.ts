export interface IAutor {
    autor_id?: number;
    nombre: string;
    apellido: string;
    fecha_nacimiento: string; // Usamos string para la fecha (puede cambiarse a Date si lo prefieres)
    nacionalidad: string;
  }
  